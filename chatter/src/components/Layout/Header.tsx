import React from 'react';
import '../../App.css';

type Props = {};

const Header = (props: Props) => {
  return (
    <header>
      <div className="header-container">
        <h1>Chatter</h1>
      </div>
    </header>
  );
};

export default Header;
